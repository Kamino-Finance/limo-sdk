/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LIMO_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ORDER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  54, 8, 208, 207, 34, 134, 239, 168,
]);

export function getUpdateOrderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ORDER_DISCRIMINATOR,
  );
}

export type UpdateOrderInstruction<
  TProgram extends string = typeof LIMO_PROGRAM_ADDRESS,
  TAccountMaker extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountOrder extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMaker extends string
        ? ReadonlySignerAccount<TAccountMaker> &
            AccountSignerMeta<TAccountMaker>
        : TAccountMaker,
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountOrder extends string
        ? WritableAccount<TAccountOrder>
        : TAccountOrder,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
  mode: number;
  value: ReadonlyUint8Array;
};

export type UpdateOrderInstructionDataArgs = {
  mode: number;
  value: ReadonlyUint8Array;
};

export function getUpdateOrderInstructionDataEncoder(): Encoder<UpdateOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mode", getU16Encoder()],
      ["value", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ORDER_DISCRIMINATOR }),
  );
}

export function getUpdateOrderInstructionDataDecoder(): Decoder<UpdateOrderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mode", getU16Decoder()],
    ["value", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getUpdateOrderInstructionDataCodec(): Codec<
  UpdateOrderInstructionDataArgs,
  UpdateOrderInstructionData
> {
  return combineCodec(
    getUpdateOrderInstructionDataEncoder(),
    getUpdateOrderInstructionDataDecoder(),
  );
}

export type UpdateOrderInput<
  TAccountMaker extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountOrder extends string = string,
> = {
  maker: TransactionSigner<TAccountMaker>;
  globalConfig: Address<TAccountGlobalConfig>;
  order: Address<TAccountOrder>;
  mode: UpdateOrderInstructionDataArgs["mode"];
  value: UpdateOrderInstructionDataArgs["value"];
};

export function getUpdateOrderInstruction<
  TAccountMaker extends string,
  TAccountGlobalConfig extends string,
  TAccountOrder extends string,
  TProgramAddress extends Address = typeof LIMO_PROGRAM_ADDRESS,
>(
  input: UpdateOrderInput<TAccountMaker, TAccountGlobalConfig, TAccountOrder>,
  config?: { programAddress?: TProgramAddress },
): UpdateOrderInstruction<
  TProgramAddress,
  TAccountMaker,
  TAccountGlobalConfig,
  TAccountOrder
> {
  // Program address.
  const programAddress = config?.programAddress ?? LIMO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    maker: { value: input.maker ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    order: { value: input.order ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("maker", accounts.maker),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("order", accounts.order),
    ],
    data: getUpdateOrderInstructionDataEncoder().encode(
      args as UpdateOrderInstructionDataArgs,
    ),
    programAddress,
  } as UpdateOrderInstruction<
    TProgramAddress,
    TAccountMaker,
    TAccountGlobalConfig,
    TAccountOrder
  >);
}

export type ParsedUpdateOrderInstruction<
  TProgram extends string = typeof LIMO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    maker: TAccountMetas[0];
    globalConfig: TAccountMetas[1];
    order: TAccountMetas[2];
  };
  data: UpdateOrderInstructionData;
};

export function parseUpdateOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      maker: getNextAccount(),
      globalConfig: getNextAccount(),
      order: getNextAccount(),
    },
    data: getUpdateOrderInstructionDataDecoder().decode(instruction.data),
  };
}

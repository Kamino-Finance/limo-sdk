/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPdaAuthorityPda } from "../pdas";
import { LIMO_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_GLOBAL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([113, 216, 122, 131, 225, 209, 22, 55]);

export function getInitializeGlobalConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_GLOBAL_CONFIG_DISCRIMINATOR,
  );
}

export type InitializeGlobalConfigInstruction<
  TProgram extends string = typeof LIMO_PROGRAM_ADDRESS,
  TAccountAdminAuthority extends string | AccountMeta<string> = string,
  TAccountPdaAuthority extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdminAuthority extends string
        ? WritableSignerAccount<TAccountAdminAuthority> &
            AccountSignerMeta<TAccountAdminAuthority>
        : TAccountAdminAuthority,
      TAccountPdaAuthority extends string
        ? WritableAccount<TAccountPdaAuthority>
        : TAccountPdaAuthority,
      TAccountGlobalConfig extends string
        ? WritableAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeGlobalConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeGlobalConfigInstructionDataArgs = {};

export function getInitializeGlobalConfigInstructionDataEncoder(): FixedSizeEncoder<InitializeGlobalConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_GLOBAL_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getInitializeGlobalConfigInstructionDataDecoder(): FixedSizeDecoder<InitializeGlobalConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeGlobalConfigInstructionDataCodec(): FixedSizeCodec<
  InitializeGlobalConfigInstructionDataArgs,
  InitializeGlobalConfigInstructionData
> {
  return combineCodec(
    getInitializeGlobalConfigInstructionDataEncoder(),
    getInitializeGlobalConfigInstructionDataDecoder(),
  );
}

export type InitializeGlobalConfigAsyncInput<
  TAccountAdminAuthority extends string = string,
  TAccountPdaAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
> = {
  adminAuthority: TransactionSigner<TAccountAdminAuthority>;
  pdaAuthority?: Address<TAccountPdaAuthority>;
  globalConfig: Address<TAccountGlobalConfig>;
};

export async function getInitializeGlobalConfigInstructionAsync<
  TAccountAdminAuthority extends string,
  TAccountPdaAuthority extends string,
  TAccountGlobalConfig extends string,
  TProgramAddress extends Address = typeof LIMO_PROGRAM_ADDRESS,
>(
  input: InitializeGlobalConfigAsyncInput<
    TAccountAdminAuthority,
    TAccountPdaAuthority,
    TAccountGlobalConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeGlobalConfigInstruction<
    TProgramAddress,
    TAccountAdminAuthority,
    TAccountPdaAuthority,
    TAccountGlobalConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LIMO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: true },
    pdaAuthority: { value: input.pdaAuthority ?? null, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.pdaAuthority.value) {
    accounts.pdaAuthority.value = await findPdaAuthorityPda({
      globalConfig: getAddressFromResolvedInstructionAccount(
        "globalConfig",
        accounts.globalConfig.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adminAuthority", accounts.adminAuthority),
      getAccountMeta("pdaAuthority", accounts.pdaAuthority),
      getAccountMeta("globalConfig", accounts.globalConfig),
    ],
    data: getInitializeGlobalConfigInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeGlobalConfigInstruction<
    TProgramAddress,
    TAccountAdminAuthority,
    TAccountPdaAuthority,
    TAccountGlobalConfig
  >);
}

export type InitializeGlobalConfigInput<
  TAccountAdminAuthority extends string = string,
  TAccountPdaAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
> = {
  adminAuthority: TransactionSigner<TAccountAdminAuthority>;
  pdaAuthority: Address<TAccountPdaAuthority>;
  globalConfig: Address<TAccountGlobalConfig>;
};

export function getInitializeGlobalConfigInstruction<
  TAccountAdminAuthority extends string,
  TAccountPdaAuthority extends string,
  TAccountGlobalConfig extends string,
  TProgramAddress extends Address = typeof LIMO_PROGRAM_ADDRESS,
>(
  input: InitializeGlobalConfigInput<
    TAccountAdminAuthority,
    TAccountPdaAuthority,
    TAccountGlobalConfig
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeGlobalConfigInstruction<
  TProgramAddress,
  TAccountAdminAuthority,
  TAccountPdaAuthority,
  TAccountGlobalConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? LIMO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: true },
    pdaAuthority: { value: input.pdaAuthority ?? null, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adminAuthority", accounts.adminAuthority),
      getAccountMeta("pdaAuthority", accounts.pdaAuthority),
      getAccountMeta("globalConfig", accounts.globalConfig),
    ],
    data: getInitializeGlobalConfigInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeGlobalConfigInstruction<
    TProgramAddress,
    TAccountAdminAuthority,
    TAccountPdaAuthority,
    TAccountGlobalConfig
  >);
}

export type ParsedInitializeGlobalConfigInstruction<
  TProgram extends string = typeof LIMO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    adminAuthority: TAccountMetas[0];
    pdaAuthority: TAccountMetas[1];
    globalConfig: TAccountMetas[2];
  };
  data: InitializeGlobalConfigInstructionData;
};

export function parseInitializeGlobalConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeGlobalConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      adminAuthority: getNextAccount(),
      pdaAuthority: getNextAccount(),
      globalConfig: getNextAccount(),
    },
    data: getInitializeGlobalConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
